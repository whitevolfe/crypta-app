import { NextComponentType, NextPageContext } from "next";
import Head from "next/head";

const Footer: NextComponentType<NextPageContext> = ({ children }) => {
  return (
    <>
      footer
    </>
  )
}

export default Footer;